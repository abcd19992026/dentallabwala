import { LoginForm } from '@/features/auth/components/LoginForm'

/**
 * LoginPage — route: /login
 * Thin page wrapper around the LoginForm feature component.
 */
export default function LoginPage() {
  return <LoginForm />
}
